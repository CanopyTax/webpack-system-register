import React from 'react';
import ReactDOM from 'react-dom';
import RootComponent from './root.component.js';

ReactDOM.render(React.createElement(RootComponent, null, null), document.getElementById('main-content'));
